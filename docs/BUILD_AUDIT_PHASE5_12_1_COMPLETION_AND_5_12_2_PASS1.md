# Build Audit — Phase 5.12.1 Completion + 5.12.2 Pass 1

## 5.12.1 completed
- rewrote the shell into a chat-first, Claude-like conversation surface
- replaced the large visible command-center wall with:
  - a compact sidebar
  - a minimal top status strip
  - hidden drawers for advanced surfaces
- moved the default feel closer to a quiet executive assistant instead of a noisy operations dashboard

## 5.12.2 started
- operations are now grouped into a dedicated drawer
- advanced maintenance/runtime/model/acceptance controls are still available, but no longer dominate the main shell surface
- maintenance and operator-heavy actions are now structurally separated from the main chat experience
