# BRAVO-1 v1.0-alpha Status

## Current posture
BRAVO-1 is now in an **alpha foundation** state.

It has:
- terminal shell
- thin local web shell with quick action controls
- local API
- session state
- active brain context
- project continuity
- capture flow
- summary writing + auto-summary policy
- runtime status + start/stop helpers + profile selection logic
- browser adapter with external + controlled-mode foundation + fetch path
- Windows adapter with status + search + focus path
- prompt-pack based operator behavior
- dependency/bootstrap planning assets
- installer/bootstrap planning groundwork

---

## What is real right now
### Working foundations
- operator loop
- slash command surface
- shell execution path
- browser open/fetch path
- controlled-browser foundation with Playwright detection and state
- runtime validation path
- structured state snapshot path
- Windows search/focus primitives

### Foundation-only lanes
- full Playwright DOM action loop
- full Windows UIA execution path
- installer build pipeline
- dependency-managed capability onboarding

---

## Why this matters
This is no longer a throwaway scaffold.
It is a reusable alpha base that can scale upward when stronger hardware arrives.

---

## Next likely priorities
1. Playwright-controlled browser lane
2. Windows UIA inspection/execution lane
3. dependency onboarding scripts
4. installer bootstrap packaging path
5. richer shell UX while preserving low noise
