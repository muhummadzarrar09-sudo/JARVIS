# BRAVO-1 runtime dependencies
# Keep the base runtime lean, but pin the capability lanes we know are coming.

playwright==1.54.0
pywinauto==0.6.9 ; platform_system == "Windows"
pyautogui==0.9.54 ; platform_system == "Windows"
